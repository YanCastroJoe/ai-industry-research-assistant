# DocFlow 面试演示手册

## 1. 启动前检查

- Windows 已安装 Python 3.11 或更高版本。
- 仓库根目录下可以创建 `.venv`；模型密钥不是必需项，未配置时会自动使用本地规则 Planner。
- 如需展示模型规划，可复制 `.env.example` 为 `.env`，并仅在本机填写 OpenAI-compatible API 配置；不要提交 `.env`。

## 2. 一键启动

在仓库根目录运行：

```powershell
.\start-demo.ps1
```

脚本会自动创建虚拟环境、补装缺失依赖并启动服务。浏览器打开：

```text
http://127.0.0.1:8010
```

如果依赖已经确认无误，可跳过依赖检查：

```powershell
.\start-demo.ps1 -SkipDependencyCheck
```

公网面试演示必须启用 `.env` 中的 `DOCFLOW_DEMO_MODE=true`、访问用户名和强密码，并通过 HTTPS 反向代理访问。应用容器默认只监听宿主机 `127.0.0.1:8010`；不要直接开放该端口，也不要在截图、README 或演示材料中展示访问密码。

## 3. 30 秒自动验收

保持服务窗口运行，另开一个 PowerShell：

```powershell
.\check-demo.ps1
```

脚本会自动跑通健康检查、Session Memory、任务创建、Planner/Runtime、引用校验、人工审核和 Markdown 导出。看到 `[PASS]` 后即可开始面试演示。

## 4. 三分钟演示流程

### 场景

团队需要把项目材料快速整理成周报、风险清单和汇报大纲，但直接调用大模型存在任务过程不可见、失败后需要整单重跑、结论缺少证据的问题。

### 推荐操作

1. 从左侧“演示模板”点击“项目周报与风险清单”，看到“已载入”提示后确认任务上下文和 Context Engineering 配置。
2. 补充或替换材料后提交任务，观察状态由 `queued`、`running` 进入 `awaiting_review`。
3. 在 Run Inspector 顶部查看“计划—执行—证据—决策”运行决策链，先说明系统为什么停在人工审核门禁。
4. 点击“执行”下钻 Trace，再从检索或引用校验步骤直接跳转到证据页；若展示故障样例，则从失败步骤进入 Checkpoint 恢复入口。
5. 结果页默认隐藏 `[E#]` 以便阅读；需要审计时打开“显示引用标记”或进入“引用”页。完成人工审核后，在结果页顶部下载完整 Markdown 产出包。
6. 打开 AgentOps 评测页，说明质量门禁来自本地任务历史，不代表生产 SLA。

### 手动输入（可选）

- 任务名称：`智能客服项目周报`
- 会话 ID：保留页面首次访问时自动生成的随机访客 ID，不要改成多人共用的固定值
- 协作偏好：`输出简洁，优先展示风险、负责人和截止时间`
- 协作目标：`基于材料生成本周项目周报、风险清单和三页汇报大纲；每条结论标注来源。`
- 材料：

```text
8月5日完成客服知识库V3升级，30条固定回归用例全部通过。8月6日发现自定义Prompt未注入文档上下文，已切回默认RAG模板并恢复来源引用。当前待办：周彦辰于8月12日前补充异常用例；李明于8月14日前完成演示环境检查。主要风险是外部模型接口波动，计划增加超时重试与降级策略。
```

### 重点讲解

1. 空状态先用居中单栏聚焦任务创建；运行后结果在同一纵向阅读流中展开并自动定位到 Run Inspector，减少左右分栏造成的注意力切换。
2. Planner 将自然语言目标拆成受白名单和依赖约束的工具计划。
3. 运行决策链把 Planner、Trace、Evidence Gate 与 Human Review 汇总成用户可下钻的审核路径；Agent Trace 继续展示每个工具的输入、尝试次数、耗时、状态和输出预览。
4. 输出同时包含周报、风险清单和三页汇报大纲；页面可隐藏 `[E#]`，但证据链与下载文件不丢失。
5. 顶部状态分别说明后端、数据库和模型配置，运行结果再单独说明本次实际采用模型还是本地规则降级；不能用“后端正常”推断“模型正常”。
6. Verifier 分开显示引用存在性、字段一致性和语义支持性；有引用不代表该引用支持结论，页面通过也只代表当前校验规则未发现异常。
7. 点击“通过并允许导出”，回到结果页下载完整产出包，完成 Agent 闭环。
8. 如需清理演示记录，可在“最近运行”或“运行记录”中点击“删除”，确认后会同时移除任务结果与 Trace；排队中和运行中的任务不可删除。

### 面试收口

该项目的核心不是单次文本生成，而是把 Planner、Tool Registry、可恢复 Runtime、Session Memory、证据校验、人工审核和评测组织成可观察、可回归的 Agent 工程闭环。

## 5. 演示失败时的最短处理

- 页面打不开：确认启动窗口仍在运行，再访问 `/health`；若需检查数据库、队列和配置状态，访问 `/ready`。
- 端口占用：运行 `.\start-demo.ps1 -Port 8011`，并访问新端口。
- 模型接口不稳定：清空 `.env` 中的 `MODEL_API_KEY`，使用确定性的本地规则模式演示。
- 数据不影响演示：任务历史保存在本地 `data/`，该目录已被 Git 忽略。
