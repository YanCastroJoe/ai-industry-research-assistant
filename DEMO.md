# DocFlow 面试演示手册

## 1. 启动前检查

- Windows 已安装 Python 3.11 或更高版本。
- 仓库根目录下可以创建 `.venv`；模型密钥不是必需项，未配置时会自动使用本地规则 Planner。
- 如需展示模型规划，可复制 `.env.example` 为 `.env`，并仅在本机填写 OpenAI-compatible API 配置；不要提交 `.env`。

## 2. 一键启动

在仓库根目录运行：

```powershell
.\start-demo.ps1
```

脚本会自动创建虚拟环境、补装缺失依赖并启动服务。浏览器打开：

```text
http://127.0.0.1:8010
```

如果依赖已经确认无误，可跳过依赖检查：

```powershell
.\start-demo.ps1 -SkipDependencyCheck
```

## 3. 30 秒自动验收

保持服务窗口运行，另开一个 PowerShell：

```powershell
.\check-demo.ps1
```

脚本会自动跑通健康检查、Session Memory、任务创建、Planner/Runtime、引用校验、人工审核和 Markdown 导出。看到 `[PASS]` 后即可开始面试演示。

## 4. 三分钟演示流程

### 场景

团队需要把项目材料快速整理成周报、风险清单和汇报大纲，但直接调用大模型存在任务过程不可见、失败后需要整单重跑、结论缺少证据的问题。

### 推荐操作

1. 从左侧选择“项目周报与风险清单”模板，确认任务上下文和 Context Engineering 配置。
2. 补充或替换材料后提交任务，观察状态由 `queued`、`running` 进入 `awaiting_review`。
3. 在 Run Inspector 中切换 Context、Plan、Trace、Evidence，定位每一步输入、工具调用和引用来源。
4. 检查生成结果与引用校验，完成人工审核后导出 Markdown。
5. 打开 AgentOps 评测页，说明质量门禁来自本地任务历史，不代表生产 SLA。

### 手动输入（可选）

- 任务名称：`智能客服项目周报`
- 会话 ID：`interview-demo`
- 协作偏好：`输出简洁，优先展示风险、负责人和截止时间`
- 协作目标：`基于材料生成本周项目周报、风险清单和三页汇报大纲；每条结论标注来源。`
- 材料：

```text
8月5日完成客服知识库V3升级，30条固定回归用例全部通过。8月6日发现自定义Prompt未注入文档上下文，已切回默认RAG模板并恢复来源引用。当前待办：周彦辰于8月12日前补充异常用例；李明于8月14日前完成演示环境检查。主要风险是外部模型接口波动，计划增加超时重试与降级策略。
```

### 重点讲解

1. 空状态先聚焦任务创建，运行后才展开执行进度和 Run Inspector，减少首次使用的信息干扰。
2. Planner 将自然语言目标拆成受白名单和依赖约束的工具计划。
3. Agent Trace 展示每个工具的输入、尝试次数、耗时、状态和输出预览。
4. 输出同时包含周报、风险清单和三页汇报大纲，每条关键结论使用 `[E#]` 引用。
5. Verifier 校验引用能否追溯到当前材料；未通过人工审核前不允许导出。
6. 点击“通过并允许导出”，再导出 Markdown，完成 Agent 闭环。

### 面试收口

该项目的核心不是单次文本生成，而是把 Planner、Tool Registry、可恢复 Runtime、Session Memory、证据校验、人工审核和评测组织成可观察、可回归的 Agent 工程闭环。

## 5. 演示失败时的最短处理

- 页面打不开：确认启动窗口仍在运行，再访问 `/health`；若需检查数据库、队列和配置状态，访问 `/ready`。
- 端口占用：运行 `.\start-demo.ps1 -Port 8011`，并访问新端口。
- 模型接口不稳定：清空 `.env` 中的 `MODEL_API_KEY`，使用确定性的本地规则模式演示。
- 数据不影响演示：任务历史保存在本地 `data/`，该目录已被 Git 忽略。
